1a: select pnr, name from mitarbeiter where geschlecht="w" && gebdat>'1900-01-01';

1b: select distinct plz from mitarbeiter where geschlecht="w" && gebdat>'1900-01-01';

1c: select pnr from pfleger where stationid in (select stationid from station where name = 'Chirurgie');

1d: select pfleger.pnr, mitarbeiter.name from pfleger inner join mitarbeiter on pfleger.pnr=mitarbeiter.pnr where mitarbeiter.ort = 'London';  
--Alternative: select pnr, name from mitarbeiter where ort = "London";

1e:select min(preis) as 'niedrigster Preis' from medikament;

1f: select name, preis from medikament where preis in(select min(preis) from medikament);

1g: select produzent, name from medikament where preis > 2*(select avg(preis) from medikament); 

1h: select distinct fachname from diagnostiziert where diagnostiziert.pnr in (select arzt.pnr from arzt);

1i: select mitarbeiter.pnr, mitarbeiter.name from mitarbeiter where mitarbeiter.pnr in (select arzt.pnr from arzt where arzt.pnr in(select pfleger.pnr from pfleger));
